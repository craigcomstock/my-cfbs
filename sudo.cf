bundle agent sudo
{
  methods:
    linux:: "_sudo";
}

bundle agent _sudo
{
  packages:
    "sudo";

  groups:
    "sudo"
      policy => "present",
      members => '{ "include": ["craig"] }';

  files:
    "/etc/sudoers"
      edit_line => lines_present("%sudo	ALL=(ALL:ALL) ALL");
}
