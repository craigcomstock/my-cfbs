body file control
{
  namespace => "personal";
}

bundle agent music
{
  methods:
      "${this.bundle}_main"
        ifvarclass => "${personal:personal.users}_${this.bundle}_task";
}

bundle agent music_main
{

  vars:
    "packages" slist =>  {
"mpv"
    };

  packages:
    linux.!termux::
      "${packages}"
        policy => "present";

  files:
}
