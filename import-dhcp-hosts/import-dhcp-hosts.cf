bundle agent import_dhcp_hosts
{
  commands:
    policy_server::
      "$(this.promise_dirname)/import-dhcp-hosts.sh $(sys.statedir)/dhcp.leases"
        if => fileexists("$(sys.statedir)/dhcp.leases"),
        contain => in_shell;
}
