bundle agent zoom
{
  methods:
    data:zoom_installed::
      "zoom_installed";
}

bundle agent zoom_installed
{
  packages:
    windows::
# found with `winget search zoom`
      "Zoom.Zoom"
        package_method => winget,
        package_policy => "add";
}
